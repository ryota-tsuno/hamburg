<div class="content_post" style="background-image: url(<%= tweet.image %>);">
    <div class="more">
        <ul class="more_list">
            <li>
              <%= link_to '詳細', "/tweets/#{tweet.id}", method: :get, class: "btn" %>
            </li>
              <% if user_signed_in? && current_user.id == tweet.user_id %>
            <li>
              <%= link_to '編集', "/tweets/#{tweet.id}/edit", method: :get, class: "btn" %>
            </li>
            <li>
              <%= link_to '削除', "/tweets/#{tweet.id}", method: :delete, class: "btn" %>
            </li>
          <% end %>
        </ul>
    </div>
      <%= simple_format(tweet.text) %>
        ここにclass指定することで投稿のtextをいじれるよ！！
</div>